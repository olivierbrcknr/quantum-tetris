function boxShape(c) {
    fill(c);
    noStroke();
    ellipse(0, 0, tileWidth - spacer, tileHeight - spacer)
}


function mapFillerShape(c) {
    fill(c);
    noStroke();
    ellipse(0, 0, tileWidth - spacer, tileHeight - spacer)
}